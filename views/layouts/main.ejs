<!DOCTYPE html>
<html class="scroll-smooth" lang="vi">
<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    
    <!-- SEO Meta Tags -->
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' - ' : '' %><%= siteName %></title>
    <meta content="<%= typeof pageDescription !== 'undefined' ? pageDescription : 'Website từ thiện Quỹ Bông Hồng Nhỏ - Little Rose Foundation' %>" name="description" />
    <meta content="<%= typeof pageKeywords !== 'undefined' ? pageKeywords : 'từ thiện, charity, foundation, quyên góp, donation' %>" name="keywords" />
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    
    <!-- Styles -->
    <link href="/dist/css/styles.css" rel="stylesheet" />
    
    <!-- Additional Head Content -->
    <%- typeof additionalHead !== 'undefined' ? additionalHead : '' %>
</head>
<body class="font-body text-text-light dark:text-text-dark bg-background-light dark:bg-background-dark">
    
    <!-- Header -->
    <%- include('../partials/header') %>
    
    <!-- Main Content -->
    <main class="min-h-screen">
        <%- body %>
    </main>
    
    <!-- Footer -->
    <%- include('../partials/footer') %>
    
    <!-- Scripts -->
    <script src="/dist/js/main.bundle.js"></script>
    
    <!-- Additional Scripts -->
    <%- typeof additionalScripts !== 'undefined' ? additionalScripts : '' %>
</body>
</html>
